# Implementation Plans

Generated by the improve skill on 2026-06-14 (updated 2026-06-23). Execute in the
order below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                | Priority | Effort | Depends on | Status |
|------|------------------------------------------------------|----------|--------|------------|--------|
| 001  | Cache GPU pipeline and shader resources across frames | P1       | S      | —          | DONE ✓ (2026-06-15) |
| 002  | Add characterization tests for math-heavy code       | P1       | M      | 001        | DONE ✓ (2026-06-15) |
| 003  | Establish README, AGENTS.md, CI, and lint config      | P1       | S      | —          | REJECTED — project docs/config are maintained directly, not via plans; CI isn't viable for this project (spirv-builder + nightly components don't run in GH Actions)
| 004  | Remove dead shader entry points                       | P2       | S      | —          | DONE ✓ (2026-06-15) |
| 005  | Add compile-time guard against host–shader struct mismatch | P2  | S      | —          | DONE ✓ (2026-06-15) |
| 006  | Remove unused `image` dependency                      | P2       | S      | —          | REJECTED — `image` is now used for PNG export (`src/main.rs:473`)
| 007  | Add interactive GUI with egui                         | P1       | L      | —          | DONE ✅ (2026-06-16) |
| 008  | Fix spiral arm formula to logarithmic spiral          | P1       | M      | —          | DONE ✅ (2026-06-22) |
| 009  | Remove dead shader functions                          | P2       | S      | —          | DONE ✅ (2026-06-22) |
| 010  | Physical star colors via spectrum-based LUT           | P1       | M      | —          | DONE ✅ (2026-06-23) |
| 011  | 3D density profiles, camera model, and ray-march      | P1       | L      | —          | DONE ✅ (2026-06-23) |
| 012  | GPU-instanced star rendering with soft point sprites  | P1       | L      | 011        | DONE ✅ (2026-06-23) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 requires 001** because the tests validate `GalaxyUniform::from_params` which may be refactored in 001. If 001 doesn't touch `from_params`, 002 can execute independently — but the ordering is safer.
- **004, 005, 006** are fully independent of each other and of 001/002.
- **007** is fully independent of all prior plans — it only touches `Cargo.toml` and `src/main.rs`, building on the now-stable compute pipeline and galaxy presets.
- **008** and **009** are independent of each other and of all prior plans.  009 removes dead 3D density helpers; 008 fixes the live `arm_modulation_2d` formula.  Recommended order: 009 first (simpler, no risk), then 008 (changes visual output).
- Recommended sequence: 009, then 008.  (The previous seven are done.)
- **012 requires 011** because the instanced star pass depends on the 3D camera model (view-proj matrix, orbit controls), the `render_mode` field on `GalaxyUniform`, and the ray-march rendering path from 011.  The star catalogue is generated on the CPU and uploaded to a storage buffer — no SPIR-V changes needed.

---

## Reconcile 2026-06-25

Last reconciled at commit **`30587d2`** (current HEAD).  All DONE plans verified against current codebase:

| Check | Result |
|-------|--------|
| `cargo check` | ✅ exit 0 |
| `cargo clippy -- -D warnings` | ✅ exit 0 |
| `cargo test` | ✅ all 59 pass |

### Verified DONE plans
- **024** — 3D glow path removed, 2D/3D unified on same star catalogue, sidebar stripped to view-only controls, 774 lines removed, 50 tests pass

- **013** — `scene_bind_group` cached in `GpuCompute`, `ensure_scene_bind_group` and `invalidate_scene_bind_group` wired in render pipeline
- **014** — `star_catalogue_uploaded` flag guards `write_buffer` calls, debug label in UI
- **015** — `needs_render = false` moved unconditionally after `queue.submit(Some(encoder.finish()))`
- **016** — `instance_count` field removed from `GpuStars`, zero references remain
- **017** — `main.rs` is 55 lines; modules: `app.rs`, `camera.rs`, `input.rs`, `render.rs`, `ui.rs`
- **018** — 7 star catalogue tests present (`deterministic`, `no_nan`, `bounds`, `extent`, `bulge_enriched`, `presets_differ`, `ngc_extent`) — all pass
- **019** — `display.wgsl` has `DisplayParams { surface_is_bgra: u32 }` uniform, conditional R↔B swap
- **020** — All camera/orbit constants named in `camera.rs` with doc comments
- **022** — `dust_tau` field on `GalaxyUniform`, host struct, `from_params`, egui slider (0.0–1.0)
- **023** — `GalaxyParams::m31()`, `m51()`, `m101()` constructors; TOML files in `galaxies/`; all presets in UI combo box; column profile tests

### Rejected this session

- **021** (Preset cross-fade) — REJECTED by owner. Instant preset switching is acceptable for this project.

---

## Audit 2026-06-25 (after plan 012 completion)

Generated by the improve skill at commit `1647567`.

### Execution order & status

| Plan | Title                                                | Priority | Effort | Depends on | Status |
|------|------------------------------------------------------|----------|--------|------------|--------|
| 013  | Cache compute bind group across frames              | P2       | S      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 014  | Upload star catalogue only when dirty               | P2       | S      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 015  | Clear `needs_render` in stars-only 3D mode           | P2       | S      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 016  | Remove dead `instance_count` field from GpuStars     | P3       | S      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 017  | Split `main.rs` monolith into modules               | P2       | L      | 013,014,015 | DONE ✅ (2026-06-25) ✓ verified |
| 018  | Add `generate_star_catalogue` characterization tests | P1       | M      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 019  | Handle surface format in display shader              | P3       | S      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 020  | Name camera and orbit magic numbers                  | P3       | S      | 017        | DONE ✅ (2026-06-25) ✓ verified |
| 021  | Preset cross-fade transition                         | P3       | M      | —          | REJECTED — owner decision; instant switching is acceptable |
| 022  | Dust extinction in the ray-march path                | P3       | S      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 023  | Add iconic galaxy presets (M31, M51, M101)           | P3       | M      | —          | DONE ✅ (2026-06-25) ✓ verified |
| 024  | Remove 3D glow, unify 2D/3D star rendering           | P1       | L      | —          | DONE ✅ (2026-06-26) ✓ verified |

### Dependency notes

- **013, 014, 015, 016, 018** are fully independent — any can execute first
- **017 requires 013, 014, 015** because they all modify `main.rs`; executing the small plans first avoids merge conflicts
- **020 requires 017** because the constants go in `camera.rs` which is created during the module split
- **019, 021, 022, 023** are independent of everything above

### Recommended execution order

1. **016** (S, trivial cleanup — warm-up)
2. **013, 014, 015** in parallel (S, three independent perf/correctness fixes)
3. **018** (M, tests for critical path)
4. **017** (L, module split — most impactful for maintainability)
5. **020** (S, constants after split)
6. **019, 021, 022, 023** in any order (all independent, all P3)

### Categories

| Category    | Plans |
|-------------|-------|
| Perf        | 013, 014 |
| Correctness | 015, 019 |
| Tech debt   | 016, 017, 020 |
| Tests       | 018 |
| Direction   | 021, 022, 023 |

---

## Findings considered and rejected

- **006 — Remove unused `image` dependency** (rejected 2026-06-15): The `image` crate was unused at plan time (commit `5a0cdfc`) but gained an active use-site at `src/main.rs:473` for saving `galaxy.png`. The dependency was bumped to `0.25.10` rather than removed.
- **003 — Establish README, AGENTS.md, CI, and lint config** (rejected 2026-06-17): Project docs and config files (README, AGENTS.md, clippy.toml, LICENSE, .env.example) are maintained directly, not through plans. CI was attempted but spirv-builder + nightly Rust components don't work in GitHub Actions on the free tier, so the workflow was removed. The handwritten artefacts (README, AGENTS.md, LICENSE, clippy.toml, .env.example) are all present and useful; they just don't belong in the plan backlog.
